<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pony.test.mapper.FundMapper">
    <select id="query" resultType="fund" parameterType="long">
        select * from fund where admin_id=#{adminId} and is_del = 0
    </select>

</mapper>